#ifndef SDL_roundedRect_h_
#define SDL_roundedRect_h_
#include <SDL2/SDL_render.h>

extern SDL_Texture* SDL_RenderFillRoundedRect(SDL_Renderer* const restrict renderer, SDL_Rect const* const restrict rect, int radius);

#endif
